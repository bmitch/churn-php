build:
  nodes:
    analysis:
      dependencies:
        before:
          - composer selfupdate --2 --no-interaction
      tests:
        override:
          - php-scrutinizer-run

checks: 
  php: true

coding_style: 
  php: 
    spaces: 
      around_operators: 
        concatenation: true
      before_parentheses:
        closure_definition: true


filter:
  paths:
    - src/
